diff --git a/dist/eject-theme/components/Search/SearchPanel.tsx b/dist/eject-theme/components/Search/SearchPanel.tsx
index 07ae1c1fa0c1a0633c26672a547408addd4c66e9..edcb2aab5887b1103336bff72cf817a10c107410 100644
--- a/dist/eject-theme/components/Search/SearchPanel.tsx
+++ b/dist/eject-theme/components/Search/SearchPanel.tsx
@@ -72,6 +72,10 @@ export function SearchPanel({ focused, setFocused }: SearchPanelProps) {
   const [searchResult, setSearchResult] = useState<MatchResult>([]);
   const searchInputRef = useRef<HTMLInputElement | null>(null);
   const [isSearching, setIsSearching] = useState(false);
+  // Compatibility fix for pinned Rspress 2.0.17 undefined.link and stale-result
+  // races. Revisit this patch when upgrading Rspress.
+  const searchPendingRef = useRef(false);
+  const searchRequestIdRef = useRef(0);
   const [resultTabIndex, setResultTabIndex] = useState(0);
   const [currentSuggestionIndex, setCurrentSuggestionIndex] = useState(0);
   const pageSearcherRef = useRef<PageSearcher | null>(null);
@@ -185,13 +189,29 @@ export function SearchPanel({ focused, setFocused }: SearchPanelProps) {
 
     const query = searchInputRef.current?.value;
     if (query) {
+      const requestId = searchRequestIdRef.current;
       const matched = await searcher.match(query);
+      if (
+        searchRequestIdRef.current !== requestId ||
+        searchInputRef.current?.value !== query
+      ) {
+        return;
+      }
       setSearchResult(matched || DEFAULT_RESULT);
+      searchPendingRef.current = false;
       setIsSearching(false);
     }
   }
 
+  const resetSearchPending = () => {
+    searchRequestIdRef.current += 1;
+    searchPendingRef.current = false;
+    setIsSearching(false);
+  };
+
   const clearSearchState = () => {
+    resetSearchPending();
+    setQuery('');
     setFocused(false);
     setResultTabIndex(0);
     setCurrentSuggestionIndex(0);
@@ -246,7 +266,13 @@ export function SearchPanel({ focused, setFocused }: SearchPanelProps) {
            * prevent enter key event when IME is composing, it's more friendly for CJK users.
            * @see https://github.com/web-infra-dev/rspress/issues/1861
            */
-          if (e.isComposing) {
+          if (!focused) {
+            return;
+          }
+          if (e.isComposing || searchPendingRef.current) {
+            return;
+          }
+          if (!searchInputRef.current?.value) {
             return;
           }
           if (
@@ -258,6 +284,9 @@ export function SearchPanel({ focused, setFocused }: SearchPanelProps) {
               normalizeSuggestions(currentSuggestions).values(),
             ).flat();
             const suggestion = flatSuggestions[currentSuggestionIndex];
+            if (!suggestion) {
+              return;
+            }
             navigate(suggestion.link);
             clearSearchState();
           }
@@ -284,9 +313,12 @@ export function SearchPanel({ focused, setFocused }: SearchPanelProps) {
 
   useEffect(() => {
     if (focused) {
+      searchPendingRef.current = false;
+      setIsSearching(false);
       setSearchResult(DEFAULT_RESULT);
       initSearch();
     } else {
+      resetSearchPending();
       setQuery('');
     }
   }, [focused]);
@@ -322,9 +354,8 @@ export function SearchPanel({ focused, setFocused }: SearchPanelProps) {
     }
   }, [lang, version, versionedSearch]);
 
-  const handleQueryChangedImpl = async (value: string) => {
+  const handleQueryChangedImpl = async (value: string, requestId: number) => {
     let newQuery = value;
-    setQuery(newQuery);
     if (newQuery) {
       const searchResult: MatchResult = [];
 
@@ -369,16 +400,34 @@ export function SearchPanel({ focused, setFocused }: SearchPanelProps) {
 
       // only setSearchResult when query is current query value
       const currQuery = searchInputRef.current?.value;
-      if (currQuery === newQuery) {
+      if (searchRequestIdRef.current === requestId && currQuery === value) {
         // Reset current suggestion index to 0 when search query changes
         setCurrentSuggestionIndex(0);
         setSearchResult(searchResult || DEFAULT_RESULT);
+        searchPendingRef.current = false;
         setIsSearching(false);
       }
     }
   };
 
   const handleQueryChange = useDebounce(handleQueryChangedImpl);
+  const handleInputChange = (value: string) => {
+    const requestId = searchRequestIdRef.current + 1;
+    searchRequestIdRef.current = requestId;
+    setQuery(value);
+    setCurrentSuggestionIndex(0);
+    setSearchResult(DEFAULT_RESULT);
+
+    if (!value) {
+      searchPendingRef.current = false;
+      setIsSearching(false);
+      return;
+    }
+
+    searchPendingRef.current = true;
+    setIsSearching(true);
+    handleQueryChange(value, requestId);
+  };
 
   const renderSearchResult = (result: MatchResult, isSearching: boolean) => {
     if (result.length === 1) {
@@ -518,7 +567,7 @@ export function SearchPanel({ focused, setFocused }: SearchPanelProps) {
                     autoComplete="off"
                     autoFocus
                     inputMode="search"
-                    onChange={e => handleQueryChange(e.target.value)}
+                    onChange={e => handleInputChange(e.target.value)}
                   />
                   <label>
                     <SvgWrapper
@@ -531,7 +580,7 @@ export function SearchPanel({ focused, setFocused }: SearchPanelProps) {
                             clearSearchState();
                           } else {
                             searchInputRef.current.value = '';
-                            setQuery('');
+                            handleInputChange('');
                           }
                         }
                       }}
diff --git a/dist/eject-theme/components/SidebarMenu/index.tsx b/dist/eject-theme/components/SidebarMenu/index.tsx
index 888dffc9d7b5e74c468a0883e9715eca49da3806..4ec3e94b8f601f0f73fb37ffb38d2cc77e16587b 100644
--- a/dist/eject-theme/components/SidebarMenu/index.tsx
+++ b/dist/eject-theme/components/SidebarMenu/index.tsx
@@ -12,6 +12,8 @@ import { forwardRef, type ReactNode, useEffect, useRef } from 'react';
 import { createPortal } from 'react-dom';
 import './index.scss';
 
+const SIDEBAR_DRAWER_QUERY = '(max-width: 768px)';
+
 /* Top Menu, only displayed on <1280px screen width */
 export const SidebarMenu = forwardRef(
   (
@@ -52,7 +54,15 @@ export const SidebarMenu = forwardRef(
     const showSidebar = sidebarConfig === true;
 
     function openSidebar() {
-      onIsSidebarOpenChange(true);
+      if (!window.matchMedia(SIDEBAR_DRAWER_QUERY).matches) {
+        closeSidebar();
+        return;
+      }
+      const next = !isSidebarOpen;
+      if (next) {
+        onIsOutlineOpenChange(false);
+      }
+      onIsSidebarOpenChange(next);
     }
 
     function closeSidebar() {
@@ -60,15 +70,25 @@ export const SidebarMenu = forwardRef(
     }
 
     function openOutline() {
-      onIsOutlineOpenChange(!isOutlineOpen);
+      const next = !isOutlineOpen;
+      if (next) {
+        onIsSidebarOpenChange(false);
+      }
+      onIsOutlineOpenChange(next);
     }
 
     function closeOutline() {
       onIsOutlineOpenChange(false);
     }
 
+    function closeOpenPanel() {
+      closeSidebar();
+      closeOutline();
+    }
+
     useEffect(() => {
       closeSidebar();
+      closeOutline();
     }, [pathname]);
 
     useEffect(() => {
@@ -77,6 +97,21 @@ export const SidebarMenu = forwardRef(
       }
     }, [hash]);
 
+    useEffect(() => {
+      const sidebarDrawer = window.matchMedia(SIDEBAR_DRAWER_QUERY);
+      const closePersistentSidebar = () => {
+        if (!sidebarDrawer.matches) {
+          onIsSidebarOpenChange(false);
+        }
+      };
+
+      closePersistentSidebar();
+      sidebarDrawer.addEventListener('change', closePersistentSidebar);
+      return () => {
+        sidebarDrawer.removeEventListener('change', closePersistentSidebar);
+      };
+    }, [onIsSidebarOpenChange]);
+
     return (
       <>
         <div
@@ -93,6 +128,9 @@ export const SidebarMenu = forwardRef(
           {showSidebar ? (
             <button
               type="button"
+              aria-controls="thq-doc-sidebar"
+              aria-expanded={isSidebarOpen}
+              aria-label={t('menuTitle')}
               onClick={openSidebar}
               className="rp-sidebar-menu__left"
             >
@@ -105,6 +143,9 @@ export const SidebarMenu = forwardRef(
           {showOutline ? (
             <button
               type="button"
+              aria-controls="thq-doc-outline"
+              aria-expanded={isOutlineOpen}
+              aria-label={t('outlineTitle')}
               disabled={outlineDisabled}
               onClick={e => {
                 e.preventDefault();
@@ -142,7 +183,7 @@ export const SidebarMenu = forwardRef(
         </div>
         {(isSidebarOpen || isOutlineOpen) &&
           createPortal(
-            <div onClick={closeSidebar} className="rp-sidebar-menu__mask" />,
+            <div onClick={closeOpenPanel} className="rp-sidebar-menu__mask" />,
             document.getElementById('__rspress_modal_container')!,
           )}
       </>
diff --git a/dist/eject-theme/components/SidebarMenu/useSidebarMenu.tsx b/dist/eject-theme/components/SidebarMenu/useSidebarMenu.tsx
index 18c11780b9874d115775bd72325493d984b3ed89..daa5389aa1c54589103fd13b962e9b5e4e33a0ad 100644
--- a/dist/eject-theme/components/SidebarMenu/useSidebarMenu.tsx
+++ b/dist/eject-theme/components/SidebarMenu/useSidebarMenu.tsx
@@ -3,6 +3,30 @@ import { type ReactNode, useEffect, useMemo, useRef, useState } from 'react';
 import { SidebarMenu } from '.';
 import { useClickOutside } from './useClickOutside';
 
+type OpenPanel = 'outline' | 'sidebar';
+
+const FOCUSABLE_SELECTOR = [
+  'a[href]',
+  'button:not([disabled])',
+  'input:not([disabled])',
+  'select:not([disabled])',
+  'textarea:not([disabled])',
+  '[tabindex]:not([tabindex="-1"])',
+].join(',');
+
+function getVisibleFocusableElements(root: HTMLElement | null) {
+  if (!root) {
+    return [];
+  }
+
+  return Array.from(
+    root.querySelectorAll<HTMLElement>(FOCUSABLE_SELECTOR),
+  ).filter(element => {
+    const style = window.getComputedStyle(element);
+    return style.display !== 'none' && style.visibility !== 'hidden';
+  });
+}
+
 function useSidebarMenu(beforeOutline?: ReactNode, afterOutline?: ReactNode) {
   const [isSidebarOpen, setIsSidebarOpen] = useState(false);
   const [isOutlineOpen, setIsOutlineOpen] = useState(false);
@@ -10,6 +34,7 @@ function useSidebarMenu(beforeOutline?: ReactNode, afterOutline?: ReactNode) {
   const sidebarMenuRef = useRef<HTMLDivElement>(null);
   const sidebarLayoutRef = useRef<HTMLDivElement>(null);
   const asideLayoutRef = useRef<HTMLDivElement>(null);
+  const lastOpenPanelRef = useRef<OpenPanel | null>(null);
 
   // only sidebar, body scroll lock can not work in iOS safari in aside
   useEffect(() => {
@@ -24,6 +49,72 @@ function useSidebarMenu(beforeOutline?: ReactNode, afterOutline?: ReactNode) {
     };
   }, [isSidebarOpen]);
 
+  useEffect(() => {
+    const openPanel: OpenPanel | null = isSidebarOpen
+      ? 'sidebar'
+      : isOutlineOpen
+        ? 'outline'
+        : null;
+    const lastOpenPanel = lastOpenPanelRef.current;
+    lastOpenPanelRef.current = openPanel;
+
+    if (!openPanel) {
+      if (!lastOpenPanel) {
+        return;
+      }
+
+      const animationFrame = window.requestAnimationFrame(() => {
+        const triggerSelector =
+          lastOpenPanel === 'sidebar'
+            ? '.rp-sidebar-menu__left'
+            : '.rp-sidebar-menu__right';
+        sidebarMenuRef.current
+          ?.querySelector<HTMLButtonElement>(triggerSelector)
+          ?.focus();
+      });
+      return () => {
+        window.cancelAnimationFrame(animationFrame);
+      };
+    }
+
+    const panel =
+      openPanel === 'sidebar'
+        ? sidebarLayoutRef.current
+        : asideLayoutRef.current;
+    const handleKeyDown = (event: KeyboardEvent) => {
+      if (event.key !== 'Tab') {
+        return;
+      }
+
+      const focusableElements = [
+        ...getVisibleFocusableElements(sidebarMenuRef.current),
+        ...getVisibleFocusableElements(panel),
+      ];
+      if (focusableElements.length === 0) {
+        return;
+      }
+
+      const activeIndex = focusableElements.indexOf(
+        document.activeElement as HTMLElement,
+      );
+      const nextIndex = event.shiftKey
+        ? activeIndex <= 0
+          ? focusableElements.length - 1
+          : activeIndex - 1
+        : activeIndex === -1 || activeIndex === focusableElements.length - 1
+          ? 0
+          : activeIndex + 1;
+
+      event.preventDefault();
+      focusableElements[nextIndex]?.focus();
+    };
+
+    document.addEventListener('keydown', handleKeyDown);
+    return () => {
+      document.removeEventListener('keydown', handleKeyDown);
+    };
+  }, [isOutlineOpen, isSidebarOpen]);
+
   useClickOutside([sidebarMenuRef, sidebarLayoutRef], () => {
     setIsSidebarOpen(false);
   });
diff --git a/dist/theme/components/Search/SearchPanel.js b/dist/theme/components/Search/SearchPanel.js
index cdab103aa4a5088f872da628a6b7851da2b33fbc..a4840dfac572a7838fb0cf57c8b9a295d2fbe639 100644
--- a/dist/theme/components/Search/SearchPanel.js
+++ b/dist/theme/components/Search/SearchPanel.js
@@ -34,6 +34,9 @@ function SearchPanel({ focused, setFocused }) {
     const [searchResult, setSearchResult] = useState([]);
     const searchInputRef = useRef(null);
     const [isSearching, setIsSearching] = useState(false);
+    // Compatibility fix for pinned Rspress 2.0.17 undefined.link and stale-result races. Revisit this patch when upgrading Rspress.
+    const searchPendingRef = useRef(false);
+    const searchRequestIdRef = useRef(0);
     const [resultTabIndex, setResultTabIndex] = useState(0);
     const [currentSuggestionIndex, setCurrentSuggestionIndex] = useState(0);
     const pageSearcherRef = useRef(null);
@@ -98,12 +101,22 @@ function SearchPanel({ focused, setFocused }) {
         setInitStatus('inited');
         const query = searchInputRef.current?.value;
         if (query) {
+            const requestId = searchRequestIdRef.current;
             const matched = await searcher.match(query);
+            if (searchRequestIdRef.current !== requestId || searchInputRef.current?.value !== query) return;
             setSearchResult(matched || DEFAULT_RESULT);
+            searchPendingRef.current = false;
             setIsSearching(false);
         }
     }
+    const resetSearchPending = ()=>{
+        searchRequestIdRef.current += 1;
+        searchPendingRef.current = false;
+        setIsSearching(false);
+    };
     const clearSearchState = ()=>{
+        resetSearchPending();
+        setQuery('');
         setFocused(false);
         setResultTabIndex(0);
         setCurrentSuggestionIndex(0);
@@ -139,10 +152,13 @@ function SearchPanel({ focused, setFocused }) {
                     }
                     break;
                 case KEY_CODE.ENTER:
-                    if (e.isComposing) return;
+                    if (!focused) return;
+                    if (e.isComposing || searchPendingRef.current) return;
+                    if (!searchInputRef.current?.value) return;
                     if (currentSuggestionIndex >= 0 && currentRenderType === RenderType.Default) {
                         const flatSuggestions = Array.from(normalizeSuggestions(currentSuggestions).values()).flat();
                         const suggestion = flatSuggestions[currentSuggestionIndex];
+                        if (!suggestion) return;
                         navigate(suggestion.link);
                         clearSearchState();
                     }
@@ -168,9 +184,14 @@ function SearchPanel({ focused, setFocused }) {
     ]);
     useEffect(()=>{
         if (focused) {
+            searchPendingRef.current = false;
+            setIsSearching(false);
             setSearchResult(DEFAULT_RESULT);
             initSearch();
-        } else setQuery('');
+        } else {
+            resetSearchPending();
+            setQuery('');
+        }
     }, [
         focused
     ]);
@@ -199,9 +220,8 @@ function SearchPanel({ focused, setFocused }) {
         version,
         versionedSearch
     ]);
-    const handleQueryChangedImpl = async (value)=>{
+    const handleQueryChangedImpl = async (value, requestId)=>{
         let newQuery = value;
-        setQuery(newQuery);
         if (newQuery) {
             const searchResult = [];
             if ("beforeSearch" in __rspack_external_virtual_search_hooks_705b6e40) {
@@ -224,14 +244,30 @@ function SearchPanel({ focused, setFocused }) {
                 await __rspack_external_virtual_search_hooks_705b6e40[key](newQuery, searchResult);
             }
             const currQuery = searchInputRef.current?.value;
-            if (currQuery === newQuery) {
+            if (searchRequestIdRef.current === requestId && currQuery === value) {
                 setCurrentSuggestionIndex(0);
                 setSearchResult(searchResult || DEFAULT_RESULT);
+                searchPendingRef.current = false;
                 setIsSearching(false);
             }
         }
     };
     const handleQueryChange = useDebounce(handleQueryChangedImpl);
+    const handleInputChange = (value)=>{
+        const requestId = searchRequestIdRef.current + 1;
+        searchRequestIdRef.current = requestId;
+        setQuery(value);
+        setCurrentSuggestionIndex(0);
+        setSearchResult(DEFAULT_RESULT);
+        if (!value) {
+            searchPendingRef.current = false;
+            setIsSearching(false);
+            return;
+        }
+        searchPendingRef.current = true;
+        setIsSearching(true);
+        handleQueryChange(value, requestId);
+    };
     const renderSearchResult = (result, isSearching)=>{
         if (1 === result.length) {
             const currentSearchResult = result[0].result;
@@ -340,7 +376,7 @@ function SearchPanel({ focused, setFocused }) {
                                         autoComplete: "off",
                                         autoFocus: true,
                                         inputMode: "search",
-                                        onChange: (e)=>handleQueryChange(e.target.value)
+                                        onChange: (e)=>handleInputChange(e.target.value)
                                     }),
                                     /*#__PURE__*/ jsx("label", {
                                         children: /*#__PURE__*/ jsx(SvgWrapper, {
@@ -351,7 +387,7 @@ function SearchPanel({ focused, setFocused }) {
                                                     e.stopPropagation();
                                                     if (query) {
                                                         searchInputRef.current.value = '';
-                                                        setQuery('');
+                                                        handleInputChange('');
                                                     } else clearSearchState();
                                                 }
                                             }
diff --git a/dist/theme/components/SidebarMenu/index.js b/dist/theme/components/SidebarMenu/index.js
index b6d0da31e7892843b8aeeaf6f6b631394a5e3527..009fa840eb91d1ecbd37d871951b25e94562271c 100644
--- a/dist/theme/components/SidebarMenu/index.js
+++ b/dist/theme/components/SidebarMenu/index.js
@@ -4,6 +4,7 @@ import { IconArrowRight, IconMenu, ReadPercent, SvgWrapper, renderInlineMarkdown
 import { forwardRef, useEffect, useRef } from "react";
 import { createPortal } from "react-dom";
 import "./index.css";
+const SIDEBAR_DRAWER_QUERY = '(max-width: 768px)';
 const SidebarMenu = /*#__PURE__*/ forwardRef(({ isSidebarOpen, onIsSidebarOpenChange, isOutlineOpen, onIsOutlineOpenChange, beforeOutline, afterOutline }, forwardedRef)=>{
     const sidebarMenuRef = useRef(null);
     const t = useI18n();
@@ -14,19 +15,32 @@ const SidebarMenu = /*#__PURE__*/ forwardRef(({ isSidebarOpen, onIsSidebarOpenCh
     const { frontmatter: { sidebar: sidebarConfig = true, outline: showOutline = true } } = useFrontmatter();
     const showSidebar = true === sidebarConfig;
     function openSidebar() {
-        onIsSidebarOpenChange(true);
+        if (!window.matchMedia(SIDEBAR_DRAWER_QUERY).matches) {
+            closeSidebar();
+            return;
+        }
+        const next = !isSidebarOpen;
+        if (next) onIsOutlineOpenChange(false);
+        onIsSidebarOpenChange(next);
     }
     function closeSidebar() {
         onIsSidebarOpenChange(false);
     }
     function openOutline() {
-        onIsOutlineOpenChange(!isOutlineOpen);
+        const next = !isOutlineOpen;
+        if (next) onIsSidebarOpenChange(false);
+        onIsOutlineOpenChange(next);
     }
     function closeOutline() {
         onIsOutlineOpenChange(false);
     }
+    function closeOpenPanel() {
+        closeSidebar();
+        closeOutline();
+    }
     useEffect(()=>{
         closeSidebar();
+        closeOutline();
     }, [
         pathname
     ]);
@@ -35,6 +49,19 @@ const SidebarMenu = /*#__PURE__*/ forwardRef(({ isSidebarOpen, onIsSidebarOpenCh
     }, [
         hash
     ]);
+    useEffect(()=>{
+        const sidebarDrawer = window.matchMedia(SIDEBAR_DRAWER_QUERY);
+        const closePersistentSidebar = ()=>{
+            if (!sidebarDrawer.matches) onIsSidebarOpenChange(false);
+        };
+        closePersistentSidebar();
+        sidebarDrawer.addEventListener('change', closePersistentSidebar);
+        return ()=>{
+            sidebarDrawer.removeEventListener('change', closePersistentSidebar);
+        };
+    }, [
+        onIsSidebarOpenChange
+    ]);
     return /*#__PURE__*/ jsxs(Fragment, {
         children: [
             /*#__PURE__*/ jsxs("div", {
@@ -47,6 +74,9 @@ const SidebarMenu = /*#__PURE__*/ forwardRef(({ isSidebarOpen, onIsSidebarOpenCh
                 children: [
                     showSidebar ? /*#__PURE__*/ jsxs("button", {
                         type: "button",
+                        "aria-controls": "thq-doc-sidebar",
+                        "aria-expanded": isSidebarOpen,
+                        "aria-label": t('menuTitle'),
                         onClick: openSidebar,
                         className: "rp-sidebar-menu__left",
                         children: [
@@ -62,6 +92,9 @@ const SidebarMenu = /*#__PURE__*/ forwardRef(({ isSidebarOpen, onIsSidebarOpenCh
                     }),
                     showOutline ? /*#__PURE__*/ jsxs("button", {
                         type: "button",
+                        "aria-controls": "thq-doc-outline",
+                        "aria-expanded": isOutlineOpen,
+                        "aria-label": t('outlineTitle'),
                         disabled: outlineDisabled,
                         onClick: (e)=>{
                             e.preventDefault();
@@ -96,7 +129,7 @@ const SidebarMenu = /*#__PURE__*/ forwardRef(({ isSidebarOpen, onIsSidebarOpenCh
                 ]
             }),
             (isSidebarOpen || isOutlineOpen) && /*#__PURE__*/ createPortal(/*#__PURE__*/ jsx("div", {
-                onClick: closeSidebar,
+                onClick: closeOpenPanel,
                 className: "rp-sidebar-menu__mask"
             }), document.getElementById('__rspress_modal_container'))
         ]
diff --git a/dist/theme/components/SidebarMenu/useSidebarMenu.js b/dist/theme/components/SidebarMenu/useSidebarMenu.js
index bcb5e74ef58571e5bc0c125fa58ae4f6f330a36f..bef417ccb531f392d8cd17542c0b2f3f0bcd2b27 100644
--- a/dist/theme/components/SidebarMenu/useSidebarMenu.js
+++ b/dist/theme/components/SidebarMenu/useSidebarMenu.js
@@ -3,12 +3,28 @@ import { clearAllBodyScrollLocks, disableBodyScroll } from "body-scroll-lock";
 import { useEffect, useMemo, useRef, useState } from "react";
 import { SidebarMenu } from "./index.js";
 import { useClickOutside } from "./useClickOutside.js";
+const FOCUSABLE_SELECTOR = [
+    'a[href]',
+    'button:not([disabled])',
+    'input:not([disabled])',
+    'select:not([disabled])',
+    'textarea:not([disabled])',
+    '[tabindex]:not([tabindex="-1"])'
+].join(',');
+function getVisibleFocusableElements(root) {
+    if (!root) return [];
+    return Array.from(root.querySelectorAll(FOCUSABLE_SELECTOR)).filter((element)=>{
+        const style = window.getComputedStyle(element);
+        return style.display !== 'none' && style.visibility !== 'hidden';
+    });
+}
 function useSidebarMenu(beforeOutline, afterOutline) {
     const [isSidebarOpen, setIsSidebarOpen] = useState(false);
     const [isOutlineOpen, setIsOutlineOpen] = useState(false);
     const sidebarMenuRef = useRef(null);
     const sidebarLayoutRef = useRef(null);
     const asideLayoutRef = useRef(null);
+    const lastOpenPanelRef = useRef(null);
     useEffect(()=>{
         sidebarMenuRef.current && isSidebarOpen && disableBodyScroll(sidebarMenuRef.current, {
             reserveScrollBarGap: true,
@@ -20,6 +36,41 @@ function useSidebarMenu(beforeOutline, afterOutline) {
     }, [
         isSidebarOpen
     ]);
+    useEffect(()=>{
+        const openPanel = isSidebarOpen ? 'sidebar' : isOutlineOpen ? 'outline' : null;
+        const lastOpenPanel = lastOpenPanelRef.current;
+        lastOpenPanelRef.current = openPanel;
+        if (!openPanel) {
+            if (!lastOpenPanel) return;
+            const animationFrame = window.requestAnimationFrame(()=>{
+                const triggerSelector = lastOpenPanel === 'sidebar' ? '.rp-sidebar-menu__left' : '.rp-sidebar-menu__right';
+                sidebarMenuRef.current?.querySelector(triggerSelector)?.focus();
+            });
+            return ()=>{
+                window.cancelAnimationFrame(animationFrame);
+            };
+        }
+        const panel = openPanel === 'sidebar' ? sidebarLayoutRef.current : asideLayoutRef.current;
+        const handleKeyDown = (event)=>{
+            if (event.key !== 'Tab') return;
+            const focusableElements = [
+                ...getVisibleFocusableElements(sidebarMenuRef.current),
+                ...getVisibleFocusableElements(panel)
+            ];
+            if (focusableElements.length === 0) return;
+            const activeIndex = focusableElements.indexOf(document.activeElement);
+            const nextIndex = event.shiftKey ? activeIndex <= 0 ? focusableElements.length - 1 : activeIndex - 1 : activeIndex === -1 || activeIndex === focusableElements.length - 1 ? 0 : activeIndex + 1;
+            event.preventDefault();
+            focusableElements[nextIndex]?.focus();
+        };
+        document.addEventListener('keydown', handleKeyDown);
+        return ()=>{
+            document.removeEventListener('keydown', handleKeyDown);
+        };
+    }, [
+        isOutlineOpen,
+        isSidebarOpen
+    ]);
     useClickOutside([
         sidebarMenuRef,
         sidebarLayoutRef
